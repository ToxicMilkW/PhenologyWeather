<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/card_bg"
    tools:context="com.iap.phenologyweather.activity.WeatherDetailActivity">

    <include layout="@layout/iclude_action_bar"/>

    <android.support.design.widget.TabLayout
        android:id="@+id/weather_detail_tabLayout"
        android:layout_width="match_parent"
        android:layout_height="36dp"
        android:background="@color/action_bar_bg_blue"
        app:tabGravity="fill"
        app:tabIndicatorColor="@color/action_bar_text_white"
        app:tabMode="fixed"
        app:tabTextColor="@color/white_40"
        app:tabSelectedTextColor="@color/action_bar_text_white"/>

    <include
        android:layout_width="match_parent"
        android:layout_height="1dp"
        layout="@layout/include_card_shadow_view"/>

    <android.support.v4.view.ViewPager
        android:id="@+id/weather_detail_viewPager"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

</LinearLayout>
